---
"@resend/chat-sdk-adapter": minor
---

Upgrade to `react-email@^6.0.0`. Replaces `@react-email/components` and `@react-email/render` with the unified `react-email` package. Bumps minimum Node to `>=20` (required by react-email 6).
